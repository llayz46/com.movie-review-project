<div class="container">
	<footer class="py-3 my-4">
		<ul class="nav justify-content-center border-bottom pb-3 mb-3">
			<li>
				<a href="{{ path('app_home') }}" class="nav-link px-2 text-body-secondary">Accueil</a>
			</li>
			<li>
				<a href="{{ path('app_movie') }}" class="nav-link px-2 text-body-secondary">Films</a>
			</li>
			{% if app.user and is_granted('ROLE_ADMIN') %}
				<li>
					<a href="{{ path('admin') }}" class="nav-link px-2 text-body-secondary">Admin</a>
				</li>
			{% endif %}
		</ul>
		<p class="text-center text-body-secondary">© 2024 Company, Inc</p>
	</footer>
</div>
